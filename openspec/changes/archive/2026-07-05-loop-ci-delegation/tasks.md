# Tasks — CHG-0018

- [x] 1.1 run-live `ci` gate → `node harness/gates/ci.mjs --force`
- [x] 1.2 Prove the delegated invocation passes end-to-end (execSync, piped
      stdio) and emits the gate event
- [x] 1.3 `npm test` + `npm run lint` green; PR; merge; archive bundle
